# Spring Boot Blog App

## How to run

1. Create a MySQL database named `blog_db`, set your DB password in `src/main/resources/application.properties`.
2. Build and run the project:
    ```
    mvn clean install
    mvn spring-boot:run
    ```
3. Visit `http://localhost:8080` for the UI.
